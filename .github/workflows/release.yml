name: Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (build and package only, skip release creation)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build and package
        run: bun run release

      - name: Verify artifacts exist
        run: |
          test -f linkedin-hide-viewed-jobs.user.js || exit 1
          test -f linkedin-hide-viewed-jobs-chrome.zip || exit 1
          test -f linkedin-hide-viewed-jobs-firefox.zip || exit 1

      - name: Get version
        id: version
        run: echo "tag=v$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Generate release notes
        id: notes
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          PREV_TAG=$(git tag --sort=-v:refname | head -1)
          RANGE="${PREV_TAG:+$PREV_TAG..HEAD}"
          NOTES="## ${{ steps.version.outputs.tag }}

          ### What's Changed

          "
          while IFS= read -r line; do
            NOTES+="- $line
          "
          done < <(git log --pretty=format:"%s" "$RANGE")
          NOTES+="
          ### Downloads

          - **Userscript**: \`linkedin-hide-viewed-jobs.user.js\`
          - **Chrome Extension**: \`linkedin-hide-viewed-jobs-chrome.zip\`
          - **Firefox Extension**: \`linkedin-hide-viewed-jobs-firefox.zip\`
          "
          echo "notes<<EOF
          $NOTES
          EOF" >> $GITHUB_OUTPUT

      - name: Create tag
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          git tag -a "${{ steps.version.outputs.tag }}" -m "Release ${{ steps.version.outputs.tag }}"
          git push origin "${{ steps.version.outputs.tag }}"

      - name: Create GitHub release
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          gh release create "${{ steps.version.outputs.tag }}" \
            linkedin-hide-viewed-jobs.user.js \
            linkedin-hide-viewed-jobs-chrome.zip \
            linkedin-hide-viewed-jobs-firefox.zip \
            --title "${{ steps.version.outputs.tag }}" \
            --notes "${{ steps.notes.outputs.notes }}" \
            --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Dry run summary
        if: ${{ github.event.inputs.dry_run == 'true' }}
        run: |
          echo "=== DRY RUN ==="
          echo "Version: $(node -p "require('./package.json').version")"
          echo ""
          echo "Files that would be attached:"
          ls -lh linkedin-hide-viewed-jobs.user.js linkedin-hide-viewed-jobs-chrome.zip linkedin-hide-viewed-jobs-firefox.zip
